{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Bienvenue sur Mon Site Symfony</h1>
    <p class="text-center">
        <a href="{{ path('app_about') }}" class="btn btn-info">À propos</a>
        <a href="{{ path('app_connexion') }}" class="btn btn-primary">Connexion</a>
        <a href="{{ path('app_inscription') }}" class="btn btn-success">Inscription</a>
    </p>
    <img src="{{ asset('images/img1.jpg') }}" alt="Logo" class="img-fluid mx-auto d-block">
{% endblock %}
